const ROUTES = {
  VERSIONS: {
    V1: "/v1",
  },
  PATHS: {
    RECORDS: "/records",
  },
  PARAMETERS: {}, // the parameters will contain values passed as part of the api or query params e.g. :someId
};

module.exports = ROUTES;
